# Network packet capture and protocol analysis
scapy>=2.5.0

# Data visualisation (charts/graphs used in the dashboard)
matplotlib>=3.7.0

# Numerical processing (arrays, fast calculations for metrics)
numpy>=1.24.0

# System and network interface statistics (process + system metrics)
psutil>=5.9.0
